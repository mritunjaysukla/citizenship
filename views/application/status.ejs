<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Application Status - Citizenship Application</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
</head>
<body>
    <main>
        <div class="form-container">
            <h2 class="section-title">Application Status</h2>
            
            <div class="status-info">
                <p><strong>Request Number:</strong> <%= application.requestNo %></p>
                <p><strong>Status:</strong> 
                    <span class="status-badge <%= application.status.toLowerCase() %>">
                        <%= application.status.replace('_', ' ') %>
                    </span>
                </p>
                <p><strong>Submitted Date:</strong> <%= application.createdAt.toLocaleDateString() %></p>
            </div>

            <% if (application.appointment) { %>
            <div class="appointment-info">
                <h3>Appointment Details</h3>
                <p><strong>Office:</strong> <%= application.appointment.officeLocation %></p>
                <p><strong>Date:</strong> <%= application.appointment.appointmentDate.toLocaleDateString() %></p>
                <p><strong>Time:</strong> <%= application.appointment.appointmentTime %></p>
            </div>
            <% } %>

            <div class="documents">
                <h3>Uploaded Documents</h3>
                <% if (application.documents && application.documents.length > 0) { %>
                    <ul class="document-list">
                    <% application.documents.forEach(doc => { %>
                        <li>
                            <i class="fas fa-file-alt"></i>
                            <%= doc.documentType %>
                            <a href="<%= doc.filePath %>" target="_blank">View</a>
                        </li>
                    <% }) %>
                    </ul>
                <% } else { %>
                    <p>No documents uploaded yet.</p>
                <% } %>
            </div>

            <div class="form-actions">
                <a href="/" class="button">
                    <i class="fas fa-home"></i> Back to Home
                </a>
            </div>
        </div>
    </main>
</body>
</html>